// Copia superficial de subarray. Dado un array de números, utiliza el método 'slice()' 
// para crear una copia superficial de una porción del array. Luego, imprime el subarray
// resultante.

const numbers = [1, 2, 3, 4, 5]
const subArray = numbers.slice(2, 4)

console.log(subArray) // resultado esperado [3, 4]... no lo entiendo 

